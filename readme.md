# Currently using:
    Python + 
    FastAPI (backend) +
    ChromaDB (local vector store) + 
    SentenceTransformer(Embeddings) + 
    ollama (local llm hosting) + 
    React(Frontend)

# Current Tiny ui version:
    Transcript -> 
    backend (fastApi) -> 
    embedding (sentence transformer) -> 
    Vector/similarity search(chrmoadb)-> 
    {Ollama => model query classify notes status} ->
    display frontend